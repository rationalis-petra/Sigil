module Spec.Glyph.Abstract.Term (term_spec) where

term_spec :: IO ()
term_spec = pure ()
-- term_spec = test_suit "eval" mktermtest
--   [-- (λx.x) 𝒰 → 𝒰
--    (id ⋅ u, u)
--    -- (λf x.f x) (λx.x) 𝒰 → 𝒰
--    (apply ⋅ id ⋅ u, u)

--   ]
--   where
--     id = [("x", un 1)] ↦ v "x"
--     apply = [("f", u → u), ("x", u)] ↦ (v "f" ⋅ v "x")

  

  
