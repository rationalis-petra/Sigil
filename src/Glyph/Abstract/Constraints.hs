module Glyph.Abstract.Constraints
  (  ) where

-- import Prettyprinter

-- import Glyph.Abstract.Syntax

-- class Default χ where
--   defalt_core :: Coreχ χ
--   default_var :: Varχ χ
--   default_uni :: Uniχ χ
--   default_prd :: Prdχ χ
--   default_abs :: Absχ χ
--   default_app :: Appχ χ

-- class Pretty (Coreχ χ) => DecPretty χ where

-- class Eq (Coreχ χ) => DecEq χ where
